services:
  - type: web
    name: tsverse-backend
    runtime: node
    plan: free
    buildCommand: PLAYWRIGHT_BROWSERS_PATH=backend/ms-playwright npm install --include=dev && npm run build && PLAYWRIGHT_BROWSERS_PATH=backend/ms-playwright npx playwright install chromium
    startCommand: npm --workspace vsmov-backend run start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: TRUST_PROXY
        value: "true"
      - key: CLIENT_URL
        value: https://tsverse.vercel.app
      - key: CORS_ORIGINS
        value: https://tsverse.vercel.app,http://localhost:5173,http://localhost:5174
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: ./ms-playwright
      - key: ENABLE_HHKUNGFU_PLAYWRIGHT
        value: "true"

  - type: web
    name: tsverse-stream-extractor
    runtime: node
    plan: free
    rootDir: stream-extractor
    buildCommand: PLAYWRIGHT_BROWSERS_PATH=./ms-playwright npm install && PLAYWRIGHT_BROWSERS_PATH=./ms-playwright npx playwright install chromium
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: ./ms-playwright
      - key: EXTRACTOR_TIMEOUT_MS
        value: "20000"
      - key: EXTRACTOR_REQUEST_TIMEOUT_MS
        value: "30000"
