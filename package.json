{
  "name": "web-phim",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend",
    "stream-extractor"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm --workspace vsmov-backend run dev\" \"npm --workspace vsmov-frontend run dev\"",
    "build": "npm --workspace vsmov-backend run build && npm --workspace vsmov-frontend run build",
    "dev:backend": "npm --workspace vsmov-backend run dev",
    "dev:frontend": "npm --workspace vsmov-frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
